import 'package:flutter/material.dart';

// UTILS: contains repeated functions, colors, or gloabl variables

const backgroundColor = Color.fromRGBO(245, 245, 245, 1.0);
const bgColor = Color.fromRGBO(0, 0, 0, 1.0);
